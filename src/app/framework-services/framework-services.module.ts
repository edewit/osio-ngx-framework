import { NgModule } from '@angular/core';

import { AboutService } from './about.service';

/**
 * A module containing framework relevant services.
 */
@NgModule({
  imports: [ ],
  exports: [ ],
  providers: [ AboutService ]
})
export class FrameworkServicesModule { }
